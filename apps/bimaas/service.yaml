apiVersion: v1
kind: Service
metadata:
  name: bimaas
  labels:
    app: bimaas
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: bimaas
